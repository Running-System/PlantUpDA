\section{Discussion}

The experimental evaluations conducted in Chapter 5 transition the theoretical trade-offs between Edge-centric and Cloud-centric architectures into concrete, empirical data. The following subsections synthesize the results of the baseline and stress tests to evaluate the "Thin Edge" architectural design of the PlantUp ecosystem.

\subsection{Validating the "Thin Edge" Architecture}

\subsubsection{Latency vs. Edge Autonomy}

The empirical results from Scenario 1 demonstrate that centralizing logical evaluation in the cloud is highly viable for the PlantUp system. The measured end-to-end latency for the Cloud-Centric variant (Architecture A) was \textbf{[INSERT LATENCY VALUE HERE] ms} during baseline operational conditions. While the Edge-Centric control variant (Architecture B) processed thresholds locally, saving the round-trip network time, the \textbf{[INSERT LATENCY VALUE HERE] ms} penalty of the Thin Edge remains negligible. The functional domain of botanical physiology—monitoring soil moisture and temperature—does not require millisecond-level actuation. Therefore, the cloud latency is imperceptible to the user, successfully validating the decision to prioritize backend analytics over localized edge autonomy.

Because all decision-making is centralized in the Supabase backend, the system inherently supports rich, cross-user social features and the Daily Mentor AI integrations. The results show that the structural benefits of this centralized, Thin Edge approach drastically outweigh the minor latency costs associated with the cloud round-trip.

\subsection{Database Performance and Resilience}

\subsubsection{Resilience Under Stress}

Scenario 2 evaluated the monolithic database architecture under a simulated disaster recovery spike of 1,000 concurrent writes. The results indicate a peak ingestion throughput of \textbf{[INSERT THROUGHPUT VALUE HERE] writes/sec}. Following the initial contention, the queue normalized within \textbf{[INSERT NORMALIZATION TIME HERE] seconds}. This confirms that the unified Supabase environment, coupled with the TimescaleDB extension for time-series ingestion, possesses sufficient structural resilience to handle intense traffic spikes without cascading failure. 

Because PlantUp does not execute strict, real-time control loops, the asynchronous latency introduced by cloud-based threshold evaluation against TimescaleDB is entirely acceptable. The system successfully absorbed the sustained burst while maintaining backend stability.

\subsection{Complexity vs. Operational Maintenance}

\subsubsection{The Trade-off of Edge Complexity}

A known architectural trade-off supported by literature is that Thin Edge systems reduce developmental complexity by centralizing logic, albeit at the cost of local device resilience \cite{zeetimThickThin, outsourceThinThick, deepwikiSupabase}. Attempting to maintain conditional logic simultaneously on the edge (C++ on the ESP32) and in the cloud (SQL/Edge Functions in Supabase) introduces significant synchronization challenges and code duplication.

The results from the load testing validate that the Supabase backend is highly resilient, successfully justifying this trade-off. By accepting the dependency on internet connectivity, PlantUp simplifies its device firmware. Modifying a moisture threshold or adjusting the gamification algorithms requires only a single backend deployment, entirely avoiding complex over-the-air (OTA) firmware updates to distributed hardware endpoints. For a consumer IoT product where rapid feature iteration and backend maintenance are critical, the empirical stability of the backend confirms that avoiding edge complexity is the correct structural approach.
